export class GridConfig {

    // Dataset
    static readonly TOTAL_ROWS = 100000;
    static readonly TOTAL_COLUMNS = 500;
    static readonly JSON_RECORDS = 50000;

    // Cell Size
    static readonly ROW_HEIGHT = 30;
    static readonly COLUMN_WIDTH = 120;

    // Minimum Resize Limits
    static readonly MIN_ROW_HEIGHT = 20;
    static readonly MIN_COLUMN_WIDTH = 50;

    // Header
    static readonly HEADER_HEIGHT = 30;
    static readonly HEADER_WIDTH = 60;

    // Summary Bar
    static readonly SUMMARY_HEIGHT = 30;

    // Font
    static readonly FONT = "14px Arial";

    // Colors
    static readonly GRID_COLOR = "#d9d9d9";
    static readonly HEADER_COLOR = "#f3f3f3";
    static readonly HEADER_TEXT = "#444";
    static readonly CELL_TEXT = "#000";

    static readonly SELECTION_COLOR = "#d8f0df";
    static readonly ACTIVE_BORDER = "#107c41";

    // Padding
    static readonly CELL_PADDING = 5;

}
